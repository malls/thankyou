# TYB-5: set up a server

Server should be written in a language that is efficient processing images, like go or rust. It will need to make API requests to the printful api based on user content. It should also serve the website.
